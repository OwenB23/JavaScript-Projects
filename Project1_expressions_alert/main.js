"use strict";

// This variable is defined as "sent1" and is concatenated with variable"sent2"
const sent1 = "This is the beginning of the string ";
const sent2 = "and this is the end of the string";

document.write(sent1 + sent2);

const alert = "Hello!";

// Widnow.alert is an alert  that pops up when the user loads the page
window.alert(alert);
document.write(a);
document.write(b);

// This is an expression
const exp = 2 + 2;
